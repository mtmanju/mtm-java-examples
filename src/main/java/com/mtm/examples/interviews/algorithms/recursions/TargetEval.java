package com.mtm.examples.interviews.algorithms.recursions;

/*
Print all possible expressions that evaluate to a target
Given a string that contains only digits from 0 to 9, and an integer value, target.
Find out how many expressions are possible which evaluate to target using binary operator +, – and * in given string of digits.
*/

public class TargetEval {

   /* public static void main(String[] args) {
        String input  ="123";
        char[] charArr=input.toCharArray();
        int target=6;
        int possibleOp=input.length() -1;
        findPossibleExpressions(charArr,target,possibleOp);

    }

    private static int findPossibleExpressions(char[] input, int target ,int posOp) {

        String[][]expressMatrix=new String[input.length+posOp];

        if(input.length==1)
            return 0;
        for(int i=)


        }



    }*/
}
